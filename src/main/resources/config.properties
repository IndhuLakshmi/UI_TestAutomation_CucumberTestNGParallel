# Framework configuration
browser=chrome
baseUrl=https://www.example.com
implicitWait=5
explicitWait=15
pageLoadTimeout=30
#parallel.threads=4
environment=qa

# External driver paths (absolute) - if provided, framework uses these executables
chromeDriverPath=
geckoDriverPath=
edgeDriverPath=
